{
  "name": "bilibili-watch-together-server",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal HTTP polling room server for Bilibili watch together MVP",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3"
  }
}
